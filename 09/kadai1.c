#include <stdio.h>
gcc -Wall search.c -o search
./search
int main()
    fprintf(stderr, "ERROR: failed to open file\n");
    int line = 0;
     if (strstr(buf, "int"))
        printf("%d:%s", line, buf);